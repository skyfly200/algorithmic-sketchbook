# Google Photos Picker API (optional). To enable the "Google Photos" button on
# Media nodes, enable the Photos Picker API in Google Cloud, create an OAuth 2.0
# Web-application Client ID with this app's origin as an authorized JavaScript
# origin, then set it here and rebuild.
VITE_GOOGLE_CLIENT_ID=
